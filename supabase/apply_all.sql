-- =====================================================================
-- 心霊マップ - apply_all.sql
-- 全マイグレーション + 全シード + スクレイプ済みデータをまとめて適用
-- 全て冪等。既存DBにも安全に流せる。
-- =====================================================================
-- 使い方:
--   Supabase ダッシュボード → SQL Editor → New query
--   このファイルの内容を貼り付け → Run
-- =====================================================================

-- このファイルは setup.sql + 全 imported_part*.sql の連結です。
-- 個別実行を推奨する場合は次の順番:
--   1. supabase/setup.sql              (schema + 2マイグレ + RLS + 基本seed + 海外seed + seed_v2)
--   2. supabase/imported_part1.sql 〜  (スクレイプ済みスポット、各 ~300件)
